docker volume create --driver local --label Nginx-tests --name nginx_test nginx_test
docker volume inspect nginx_test
docker run -it -h ngx --name nginx-test -p 80:80 --mount source=nginx_test,destination=/u01 ubuntu:22.04
docker start nginx-test
docker exec -it nginx-test bash
apt-get update
apt-get install curl
apt-get install net-tools
apt-get install mc
apt-get install nginx

nginx 
ps -ef
curl http://localhost
nginx -s stop

#
docker cp . nginx-test:/u01/
docker cp nginx-test:/u01/ ..
cp . nginx-test:/u01/
#

cd /etc/nginx
cp -p nginx.conf /u01/nginx.conf.ORIG

правка файла 
cp -p nginx.conf /u01/nginx.conf

cd sites-enabled
ls -l
rm default
cp /etc/nginx/sites-available/default test.conf 
mkdir -p /u01/www/html
cp /var/www/html/index.nginx-debian.html /u01/www/html/index.html

правка файлов
проверка
 
cp -p test.conf /u01/test.conf.01

nginx -s  reload
cp -p test.conf /u01/test.conf.02


